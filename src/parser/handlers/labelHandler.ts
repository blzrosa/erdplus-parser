import { LabelNode, RelationalEdge, RelationalNode } from '../../core/types';

// Currently a stub, but structured for future logic
export function handleLabelNodes(
    nodes: Map<string, LabelNode>, 
    [nodeMap, edgeMap]: [Map<string, RelationalNode>, Map<string, RelationalEdge>]
) {
    return;
}